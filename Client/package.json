{
  "name": "svgworkspacepublicretained-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "dotnet tool restore && dotnet fable watch . -o output --noCache --run vite",
    "build": "dotnet tool restore && dotnet fable . -o output --noCache && vite build"
  },
  "dependencies": {
    "@microsoft/signalr": "10.0.0"
  },
  "devDependencies": {
    "vite": "7.1.3"
  }
}
