interface:
  display_name: "Work Board"
  short_description: "Burn down one coordination-wired workspace board"
  default_prompt: "Use $work-board to reconcile and burn down one coordination-wired product workspace board."

policy:
  allow_implicit_invocation: false
