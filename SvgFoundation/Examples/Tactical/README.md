# Tactical example

This bundle retains the disclosed neutral tactical scene as editable product content. It
demonstrates stable semantic identities, layers, camera state, selection, and focus without
depending on an undisclosed application or repository.

Edit `scene.json`, then import or reproduce those values through Studio's Arrange and Play modes.
The adapter in `SvgFoundation/TacticalCompatibility.fs` owns the disclosed projection boundary.
